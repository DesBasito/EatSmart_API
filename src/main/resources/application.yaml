server:
  port: 9090

spring:
  application:
    name: EatSmart API
  datasource:
    url: jdbc:h2:./db/eat_smart;AUTO_SERVER=true
    username: root
    password: troot
    driver-class-name: org.h2.Driver
  liquibase:
    change-log: classpath:db/changelog/master.yml

